{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for DBSync Studio. Dump and restore processes are spawned by the Rust engine, not by the webview, so no shell permission is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
